<div id="photo_<%= photo.id %>">
  <%= image_tag photo.image.thumb, alt: photo.title %>
  <%= link_to 'Редактировать', edit_admin_photo_path(photo.id) %>
  <%= link_to 'Удалить', admin_photo_path(photo.id), method: :delete %>
</div>
