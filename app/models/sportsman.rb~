class Sportsman < ActiveRecord::Base
  attr_accessible :name, :phone
  
  belongs_to :user
  has_many :subs, dependent: :destroy
  
  def self.active
    joins(:subs).active
  end
  
  def self.inactive
    joins(:subs).inactive
  end
end
